# Grid AI frontend

React + Vite dashboard for the Grid AI emergency recovery prototype.

Run after the backend is running:

```bash
npm install
npm run dev
```

The UI expects the FastAPI backend at `http://localhost:8000`.
